function main() {
	sf::init("build/libltnsfml.so");
	var window = sf::window::open();
	while(sf::window::is_open(window)) {
		sf::window::clear(window, 0xff000000);
		sf::window::display(window);
	}
	sf::window::destroy(window);
}